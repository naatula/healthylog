<nav class="navbar navbar-expand-sm navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="/">Healthylog</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-3 mb-sm-0">
        <% if(data.loggedInAs){ %> 
          <li class="nav-item">
            <a class="nav-link <% if(data.page==='summary'){ %>active<% } %>" href="/behavior/summary">My summary</a>
          </li>
          <li class="nav-item">
            <a class="nav-link <% if(data.page==='reporting'){ %>active<% } %> " href="/behavior/reporting">Reporting</a>
          </li>
        <% } %> 

      </ul>
      <div class="button-toolbar">
        <% if(data.loggedInAs){ %>
          <a class="btn btn-outline-secondary btn-logout" href="/auth/logout"><span class="logout-email"><%= data.loggedInAs %></span><span class="logout-prompt">Log out</span></a>
        <% } else { %>
          <a class="btn btn-outline-success" href="/auth/registration">Sign up</a>
          <a class="btn btn-primary" href="/auth/login">Log in</a>
          <% } %> 
      </div>
    </div>
  </div>
</nav>
